# 含 API Key 的配置可放入 .env（可选）
.env
.env.local
# 用户实际配置（含 API Key 或本地选择），不提交
config/api.json
config/secrets.json
config/mood_state.json
# 天气模块：IP 定位后记住的城市（运行时生成），不提交
config/weather.json
*.pyc
__pycache__/
.idea/
dist/
build/
*.spec.bak
