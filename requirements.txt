PyQt5>=5.15
PyQtWebEngine>=5.15
pynput>=1.7.6
openai>=1.0.0
requests>=2.28.0
send2trash>=1.8.0

# Windows 专属依赖（PEP 508 环境标记：macOS/Linux 上 pip 会自动跳过，
# 否则 GitHub Actions 的 macOS 构建会在安装依赖阶段因 pywin32 无 macOS wheel 而失败）
uiautomation>=2.0; sys_platform == 'win32'
pywin32>=300; sys_platform == 'win32'
keyboard>=0.13.5; sys_platform == 'win32'

# 可选：讯飞星火
# sparkai
